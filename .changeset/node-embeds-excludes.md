---
'@astroscope/node': minor
---

embed exclude patterns at `@astroscope/node/excludes` (matching via `@entwico/dash/match`, adds `suffix`/`includes` pattern types) — replaces the removed `@astroscope/excludes`
