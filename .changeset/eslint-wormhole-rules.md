---
'@astroscope/eslint-plugin': minor
---

new opt-in `configs.wormhole` rule set for `@astroscope/wormhole` projects: `wormholes-static-access` (dynamic access/aliasing defeats payload slicing), `server-readonly` (`set`/`subscribe` in astro server code), and `no-use-wormhole-in-astro`
