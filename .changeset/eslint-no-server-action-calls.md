---
'@astroscope/eslint-plugin': minor
---

new `no-server-action-calls` rule (in `recommended`): forbids calling actions — directly or via `Astro.callAction()` — during server rendering of `.astro` files
