---
'@astroscope/node': minor
---

add native mounts (`mountNativeHandler` from `@astroscope/node/native`): raw `(req, res)` handlers dispatched before static/astro, identical in dev and prod — for `oidc-provider` and other http-native libraries
