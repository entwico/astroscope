---
'@astroscope/i18n': minor
---

the client payload is now sliced per page: each island carries only its own chunk hashes, translation chunks preload alongside component chunks, and the whole-site imports map is gone
