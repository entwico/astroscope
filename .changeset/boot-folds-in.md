---
'@astroscope/node': minor
---

embed the boot lifecycle and dev restart machinery — replaces `@astroscope/boot` (`BootContext` is now exported from `@astroscope/node`, boot events from `@astroscope/node/events`)
