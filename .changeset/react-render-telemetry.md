---
'@astroscope/react': minor
---

island render telemetry: `astro.island.render.duration` (component, `string`/`stream` path), `astro.island.check.duration` for the detection probe and `astro.island.render.failures` (`root`/`boundary`)
