---
"@astroscope/react": major
---

new package: `@astrojs/react` wrapper that renders islands with `renderToString`, streaming an island only when it suspends on the server; includes SSR effect stripping
