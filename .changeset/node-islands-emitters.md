---
'@astroscope/node': minor
---

new `@astroscope/node/islands` API for integrations: `registerIslandEmitter` and `registerDocumentEmitter` contribute per-island preload links/scripts and per-document content to the html streaming pass (used by `@astroscope/i18n` and `@astroscope/wormhole`)
